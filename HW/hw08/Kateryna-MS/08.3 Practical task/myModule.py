def rectangle_area(a, b):
    S_r = round(a * b, 2)
    return S_r

def triangle_area(h, a):
    S_t = round(0.5 * h * a, 2)
    return S_t

def circle_area(pi, r):
    S_c = round(pi * pow(r, 2), 2)
    return S_c
